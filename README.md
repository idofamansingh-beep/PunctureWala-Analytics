# PunctureWala Analytics

The source data is in `data/bookings.csv`. Open `PunctureWala_Bookings_Analysis.ipynb` in VS Code or Jupyter and select the project virtual-environment kernel.

The notebook loads the bookings dataset and checks its shape, columns, inferred data types, missing values, duplicate rows, and booking-status distribution.
